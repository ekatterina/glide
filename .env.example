# Public Mapbox token — required for the interactive night-style map.
# Get one free at https://account.mapbox.com/access-tokens/
# Without this token the app falls back to a static SVG preview.
VITE_MAPBOX_TOKEN=
